// STL includes
#include <vector>
#include <string>

// C includes

// Library includes
#include <cxxtest/TestSuite.h>

// Project includes
#include <Parser.h>
#include <Scheduler.h>

using namespace std;

class SchedulerTestSuite: public CxxTest::TestSuite
{
  Scheduler sched;
public:
  void setUp()
  {
  }

  void testEmpty( void )
  {
  }
  void testAdd( void )
  {
    sched.add(1000,Parser("test"));
  }
};
// vim:cin:ai:sts=2 sw=2 ft=cpp
